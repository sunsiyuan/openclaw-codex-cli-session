{
  "name": "codex-cli-session",
  "version": "0.1.0",
  "description": "OpenClaw ACP runtime plugin that delegates session turns to local Codex CLI",
  "type": "module",
  "private": true,
  "openclaw": {
    "extensions": [
      "./src/index.js"
    ]
  },
  "scripts": {
    "smoke": "node ./scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
